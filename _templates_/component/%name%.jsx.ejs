/**
 * <%=username%>
 */

import React from 'react';

import './<%=name%>.scss';

const pkg = require('<%=path('package.json')%>');

const <%=cz(name)%>Component = () => {
	console.info(`App version: ${pkg.version}`);
	
	return (
		<div className="<%=name%>">
			<%=cz(name)%> works fine!
		</div>
	);
}

export default <%=cz(name)%>Component;